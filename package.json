{
  "name": "election-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "cd ../markdown_table_creator && npm run electron && cd ../electron-quick-start && electron .",
    "build": "electron-packager . 日记生成器  --platform=mas --arch=all  --out=output/ --overwrite --ignore=node_modules/electron-* --ignore=node_modules/.bin --ignore=.git --ignore=output --prune"
  },
  "repository": "",
  "keywords": [
    "Electron",
    "justyan"
  ],
  "author": "justyan",
  "license": "MIT",
  "devDependencies": {
    "electron": "^2.0.0"
  },
  "dependencies": {
    "mongoose": "^5.2.2"
  }
}
